<!DOCTYPE html>
<html>
<head>
    <title>Breast Cancer Predictor</title>
    <style>
        body { font-family: Arial, sans-serif; max-width: 600px; margin: auto; padding: 2em; }
        label { display: block; margin-top: 1em; }
        input[type="number"] { width: 100%; padding: 0.5em; }
        input[type="submit"] { margin-top: 1em; padding: 0.5em 1em; }
        .result { margin-top: 1.5em; font-size: 1.2em; font-weight: bold; color: #444; }
    </style>
</head>
<body>
    <h1>Breast Cancer Prediction App</h1>
    <form action="/predict" method="POST">
        <label>Clump Thickness:
            <input type="number" name="Clump_thickness" step="any" value="{{ Clump_thickness or '' }}" required>
        </label>
        <label>Uniformity of Cell Size:
            <input type="number" name="Uniformity_of_cell_size" step="any" value="{{ Uniformity_of_cell_size or '' }}" required>
        </label>
        <label>Uniformity of Cell Shape:
            <input type="number" name="Uniformity_of_cell_shape" step="any" value="{{ Uniformity_of_cell_shape or '' }}" required>
        </label>
        <label>Marginal Adhesion:
            <input type="number" name="Marginal_adhesion" step="any" value="{{ Marginal_adhesion or '' }}" required>
        </label>
        <label>Single Epithelial Cell Size:
            <input type="number" name="Single_epithelial_cell_size" step="any" value="{{ Single_epithelial_cell_size or '' }}" required>
        </label>
        <input type="submit" value="Predict">
    </form>

    {% if prediction is defined %}
    <div class="result">
        Prediction: <strong>{{ prediction }}</strong>
    </div>
    {% endif %}
</body>
</html>
